import { useEffect, useRef } from "react";

const Canvas = () => {
  const canvasRef = useRef()
  
  useEffect(() => {

  })

  return (
    <canvas ref={canvasRef}>

    </canvas>
  );
};

export default Canvas;